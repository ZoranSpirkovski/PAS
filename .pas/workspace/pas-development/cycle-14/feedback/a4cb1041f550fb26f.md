[OQI-01]
Target: skill:check-self-eval
Degraded: Hook script crashes with `[: 0\n0: integer expression expected` before delivering its message
Root Cause: Numeric comparison in `check-self-eval.sh` line 39 receives a multi-line string (likely two newline-separated counts) instead of a single integer
Fix: Quote the variable and/or collapse to a single integer (e.g., `wc -l < file` rather than `wc -l file | awk`, or pipe through `tr -d '\n'`); add explicit numeric coercion before `[ ... -gt ... ]`
Evidence: Stop hook output: "/home/zoran/.claude/plugins/cache/pas-framework/pas/1.3.2/hooks/check-self-eval.sh: line 39: [: 0\n0: integer expression expected"
Priority: HIGH

[OQI-02]
Target: agent:dx-specialist
Degraded: Self-evaluation file was not written before initial shutdown attempt
Root Cause: Spawn directive said self-eval is required "before returning your final result," but agent only completed the READY handshake and waited for instructions — no productive work occurred yet, so the agent did not anticipate that the Stop hook fires on idle/shutdown regardless of whether work was done
Fix: Agent should write `No issues detected.` to the feedback file immediately after the READY handshake when no work has been performed, OR the spawn protocol should clarify that self-eval is required before ANY shutdown (including idle shutdown post-handshake)
Evidence: Stop hook fired after READY handshake with no intervening work; agent had to be re-prompted to write self-eval
Priority: MEDIUM

[OQI-03]
Target: process:pas-development
Degraded: Auto-broadcast of orchestrator's `[PAS] Phase: ...` task list to all teammates created confusion (task_assignment notifications shaped to look like work assignments) and required a clarifying broadcast from team-lead. Each teammate also received per-call "task tools haven't been used recently" reminders that did not apply to them.
Root Cause: When team-lead claims orchestrator-only PAS lifecycle bookkeeping tasks, the team task system broadcasts `task_assignment` notifications to every teammate; teammates have no automatic way to distinguish orchestrator-only tasks from real assignments.
Fix: Either (a) suppress task broadcasts for tasks prefixed `[PAS]` (these are orchestrator-only by convention), or (b) document this collision in the dispatch / lifecycle skill so the orchestrator pre-warns teammates in their spawn directive ("ignore any task notifications shaped `[PAS] ...`"), or (c) namespace orchestrator tasks in a parallel channel that teammates do not subscribe to.
Evidence: team-lead broadcast: "if you received system notifications shaped like `{type:task_assignment,subject:[PAS] ...}` — ignore them. Those 8 tasks are orchestrator-only PAS lifecycle bookkeeping; they got auto-broadcast when I claimed ownership, which was a tooling artifact, not an instruction."
Priority: MEDIUM
